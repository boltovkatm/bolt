const {pool,send,body,setup,validNick}=require('./lib');module.exports=async(req,res)=>{if(req.method!=='POST')return send(res,405,{error:'Method not allowed'});try{await setup();const d=body(req);if(!validNick(d.nickname)||!d.role)return send(res,400,{error:'Заполните обязательные поля.'});await pool.query('INSERT INTO applications(role,payload) VALUES($1,$2)',[d.role,d]);send(res,201,{ok:true});}catch{send(res,500,{error:'Сервер временно недоступен.'});}};
